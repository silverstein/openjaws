CONVEX_SITE_URL="https://your-convex-site.convex.site"
NEXT_PUBLIC_CONVEX_URL="https://your-convex-deployment.convex.cloud"

# AI keys
ANTHROPIC_API_KEY="your-anthropic-key"                # required only if not using gateway
GOOGLE_GENERATIVE_AI_API_KEY="your-google-genai-key"  # required only if not using gateway
AI_GATEWAY_API_KEY="your-vercel-ai-gateway-key"       # set to route all models through Vercel AI Gateway
# AI_GATEWAY_ENDPOINT="https://ai-gateway.vercel.sh/v1/ai" # optional override (self-hosted/custom)
NEXT_PUBLIC_SHARK_MODEL="claude-4.5-sonnet" # override if your gateway uses a different alias
ELEVENLABS_API_KEY="your-elevenlabs-key"

# Convex dev deployment (used by npx convex dev)
CONVEX_DEPLOYMENT="dev:your-deployment-id"
